from setuptools import setup

setup(
    name='beets-albumsort',
    version='0.3.0',
    description='beets plugin to sort albums chronologically',
    license='MIT',
    packages=['beetsplug'],
    install_requires=['beets'],
    classifiers=[],
)
